# ── Python ─────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
build/
dist/

# ── Virtual environments / secrets ─────────────────────────────────────
.venv/
.env

# ── Generated runtime output (keep the folders, drop the contents) ─────
runtime/audio/*
!runtime/audio/.gitkeep
logs/*
!logs/.gitkeep
sessions/*
!sessions/.gitkeep

# ── Large binaries fetched on demand, not source ───────────────────────
# Piper voice models. Re-download with:
#   python -m piper.download_voices --download-dir ./voices <name>
voices/

# ── Local-only working directories ─────────────────────────────────────
.claude/
NAO-Video/

# Developer log — kept locally, not published
CLAUDE.md

# Build plan — kept locally, not published
implementationPlan.md
